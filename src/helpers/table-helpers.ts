import { Song } from "../models/table-models";

// export function createRows(amount: number): Song[] {
//     return [...Array(amount).keys()].map(n => makeSongObject(n))
// }

// function makeSongObject(id: number): Song {
//     return { id: id, artist: (Math.random() + 1).toString(36).substring(7), title: (Math.random() + 1).toString(36).substring(7) }
// }
